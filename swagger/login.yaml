consumes:
  - application/json
parameters:
  - name: authentication
    in: body
    properties:
      username:
        type: string
      password:
        type: string
responses:
  200:
    description: Authenticated
  401:
    description: Unauthorized
